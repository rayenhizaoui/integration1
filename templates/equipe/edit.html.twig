{% extends 'baseBack.html.twig' %}

{% block title %}Modifier équipe{% endblock %}

{% block body %}
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body">
                    <h1>Modifier l'équipe</h1>

                    {{ include('equipe/_form.html.twig', {'button_label': 'Modifier'}) }}

                    <a href="{{ path('app_equipe_index') }}">Retour</a>

                    {{ include('equipe/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
