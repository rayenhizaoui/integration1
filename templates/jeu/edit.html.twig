{% extends 'baseBack.html.twig' %}

{% block title %}Modifier jeu{% endblock %}

{% block body %}
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body">
                    <h1>Modifier le jeu</h1>

                    {{ include('jeu/_form.html.twig', {'button_label': 'Modifier'}) }}

                    <a href="{{ path('app_jeu_index') }}">Retour</a>

                    {{ include('jeu/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
