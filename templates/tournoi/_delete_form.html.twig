<form method="post" action="{{ path('app_tournoi_delete', {'id': tournoi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tournoi.id) }}">
    <button style="font-size:24px ; border-color: transparent ; background-color: transparent"  > <i class="fa fa-trash-o" style="color:red"></i></button>
</form>
